<table id="credits-table" cellpadding="0" cellspacing="0">
<% credits.each do |credit| -%>
  <tr>
    <td class="name">
      <%= link_to credit.name, 
                  edit_goal_credit_path(credit.goal, credit) %>
    </td>
    <td class="amount">
      <%= number_to_currency(credit.amount) %>
    </td>
    <td class="delete">
      <%= link_to 'x', goal_credit_url(goal, credit),
                       :method  => :delete,
                       :remote  => true, 
                       :class => 'destructive' %>
    </td>
  </tr>
<% end -%>
</table>